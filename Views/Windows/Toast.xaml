<Window x:Class="Baru_Client.Views.Windows.Toast"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        Width="300" Height="80">

    <Border Background="#DD333333" CornerRadius="8" Padding="10" >
        <Grid>
            <TextBlock x:Name="MessageTextBlock"
                       Foreground="White"
                       FontSize="16"
                       Text="Toast message here"
                       TextWrapping="Wrap"
                       VerticalAlignment="Center"
                       Margin="0,0,30,0" />
            <Button Width="35" Height="30"
                        HorizontalAlignment="Right" VerticalAlignment="Top"
                        Background="Transparent" BorderThickness="0"
                        Cursor="Hand"
                        Click="CloseButton_Click">
                                <TextBlock Text="X" Foreground="White" FontWeight="Bold" FontSize="20" 
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Button>

        </Grid>
    </Border>

</Window>
