<Page
    x:Class="Baru_Client.Views.Pages.AccuracyPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Baru_Client.Views.Pages"
    xmlns:viewmodels="clr-namespace:Baru_Client.ViewModels.Pages"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
    mc:Ignorable="d"
    Title="정확도 그래프" Loaded="Page_Loaded">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 제목 -->
        <ui:TextBlock Grid.Row="0" Text="정확도 그래프" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,20"/>

        <!-- 날짜 선택 -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" VerticalAlignment="Top" Margin="0,0,0,20">
            <TextBlock Text="날짜 선택:" FontWeight="Bold" VerticalAlignment="Center"/>
            <DatePicker SelectedDate="{Binding SelectedDate, Mode=TwoWay}" Margin="10,0,0,0"/>
            <TextBlock Text="{Binding SelectedDateString}" Margin="20,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- 정확도 라인차트 -->
        <lvc:CartesianChart Grid.Row="2" Series="{Binding AccuracySeries}" Margin="0,0,0,0" LegendLocation="Right" Hoverable="True">
            <lvc:CartesianChart.AxisX>
                <lvc:Axis Title="횟수" FontSize="12" FontWeight="Bold" />
            </lvc:CartesianChart.AxisX>
            <lvc:CartesianChart.AxisY>
                <lvc:Axis Title="정확도 (%)" MinValue="0" MaxValue="100" FontSize="12" FontWeight="Bold"/>
            </lvc:CartesianChart.AxisY>
        </lvc:CartesianChart>
    </Grid>
</Page>
