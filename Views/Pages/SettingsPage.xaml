<Page
    x:Class="Baru_Client.Views.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="clr-namespace:Baru_Client.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    mc:Ignorable="d">

    <Page.Resources>
        <helpers:EnumToBooleanConverter x:Key="EnumToBooleanConverter" />
    </Page.Resources>

    <StackPanel Margin="36">
        <TextBlock FontSize="22" FontWeight="Bold" Text="설정"/>
        <TextBlock Margin="0,12,0,0" Text="테마" FontSize="16"/>
        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
            <RadioButton
                Command="{Binding ChangeThemeCommand}"
                CommandParameter="theme_light"
                Content="기본"
                GroupName="themeSelect"
                IsChecked="{Binding CurrentTheme, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Light, Mode=OneWay}"/>
            <RadioButton
                Command="{Binding ChangeThemeCommand}"
                CommandParameter="theme_dark"
                Content="다크모드"
                GroupName="themeSelect"
                IsChecked="{Binding CurrentTheme, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Dark, Mode=OneWay}"/>
        </StackPanel>
        <TextBlock Margin="0,24,0,0" FontSize="20" FontWeight="Medium" Text="{Binding AppVersion, Mode=OneWay}" />
        <ui:Arc Height="100" Stroke="Black" Width="100" Margin="0,20,0,0"/>
    </StackPanel>
</Page>
